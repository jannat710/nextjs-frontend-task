/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'png.pngtree.com',
            'img.freepik.com'
        ]
    }
};

export default nextConfig;
